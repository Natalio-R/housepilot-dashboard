import React from "react";
import { SignInForm } from "../../routes";

const Login = () => {
  return (
    <div className="w-full h-full flex items-center justify-center">
      <SignInForm />
    </div>
  );
};

export default Login;
